<Project Sdk="Microsoft.VisualStudio.JavaScript.Sdk/0.5.128-alpha">
  <PropertyGroup>
    <!-- Defines how to launch the app -->
    <StartupCommand>npm start</StartupCommand>
    <!-- Defines where the build output goes -->
    <BuildOutputFolder>$(MSBuildProjectDirectory)\build</BuildOutputFolder>
  </PropertyGroup>
</Project>